de.nothinglefttolearn.com {
    encode gzip

    # API — proxy to the Go backend
    handle /mixdown* {
        reverse_proxy localhost:8000
    }

    handle /stems* {
        reverse_proxy localhost:8000
    }

    # Static site
    handle {
        root * /opt/difference-engine/frontend
        file_server
    }
}
